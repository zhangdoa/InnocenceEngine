// shadertype=glsl
#version 330 core

in vec2 TexCoords;
out vec4 FragColor;

uniform sampler2D finalColor;

void main()
{       
	//gamma correction
	vec3 gammaCorrectedfinalColor = pow(texture(finalColor, TexCoords).rgb, vec3(1.0 / 2.2));

	FragColor = vec4(gammaCorrectedfinalColor, 1.0);
}
