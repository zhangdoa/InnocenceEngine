#version 330 core

layout (location = 0) in vec3 in_Position;
layout (location = 1) in vec2 in_TexCoord;

uniform mat4 uni_MVP;

out vec2 thefrag_TexCoord;

void main()
{
	gl_Position = uni_MVP * vec4(in_Position, 1.0);
	thefrag_TexCoord = vec2(in_TexCoord);
}