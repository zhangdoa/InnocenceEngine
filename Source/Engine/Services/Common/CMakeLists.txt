file(GLOB HEADERS "*.h")
file(GLOB SOURCES "*.cpp")
add_library(GraphicsServiceCommon ${HEADERS} ${SOURCES})
set_property(TARGET GraphicsServiceCommon PROPERTY POSITION_INDEPENDENT_CODE ON)
set_target_properties(GraphicsServiceCommon PROPERTIES FOLDER Engine/GraphicsService)