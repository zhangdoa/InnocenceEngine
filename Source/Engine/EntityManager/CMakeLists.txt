file(GLOB HEADERS "*.h")
file(GLOB SOURCES "*.cpp")
add_library(EntityManager ${HEADERS} ${SOURCES})
set_property(TARGET EntityManager PROPERTY POSITION_INDEPENDENT_CODE ON)
set_target_properties(EntityManager PROPERTIES FOLDER Engine)