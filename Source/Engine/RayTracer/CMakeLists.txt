file(GLOB HEADERS "*.h")
file(GLOB SOURCES "*.cpp")
add_library(RayTracer ${HEADERS} ${SOURCES})
set_property(TARGET RayTracer PROPERTY POSITION_INDEPENDENT_CODE ON)
set_target_properties(RayTracer PROPERTIES FOLDER Engine)