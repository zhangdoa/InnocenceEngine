aux_source_directory(. DIR_SRCS)
add_library(STBWrapper ${DIR_SRCS})
set_property(TARGET STBWrapper PROPERTY POSITION_INDEPENDENT_CODE ON)
set_target_properties(STBWrapper PROPERTIES FOLDER ThirdParty)
target_include_directories(STBWrapper PRIVATE "${INNO_INCLUDE_DIRECTORIES}/stb")