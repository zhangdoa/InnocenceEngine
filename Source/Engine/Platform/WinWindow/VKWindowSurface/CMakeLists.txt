file(GLOB HEADERS "*.h")
file(GLOB SOURCES "*.cpp")
add_library(WinVKWindowSurface ${HEADERS} ${SOURCES})
set_target_properties(WinVKWindowSurface PROPERTIES FOLDER Engine/Window)
target_include_directories(WinVKWindowSurface PRIVATE "${INNO_GITSUBMODULE_DIRECTORIES}/Vulkan-Headers/include")