file(GLOB HEADERS "*.h")
file(GLOB SOURCES "*.cpp")
add_library(VKRenderingServer ${HEADERS} ${SOURCES})
set_property(TARGET VKRenderingServer PROPERTY POSITION_INDEPENDENT_CODE ON)
set_target_properties(VKRenderingServer PROPERTIES FOLDER Engine/RenderingServer)
target_include_directories(VKRenderingServer PRIVATE "${INNO_SUBMODULE_DIRECTORIES}/Vulkan-Headers/include/")
target_link_libraries(VKRenderingServer ${Vulkan_LIBRARIES})