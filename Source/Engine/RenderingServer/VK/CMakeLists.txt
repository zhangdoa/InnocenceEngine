file(GLOB HEADERS "*.h")
file(GLOB SOURCES "*.cpp")
add_library(InnoVKRenderingServer ${HEADERS} ${SOURCES})
set_property(TARGET InnoVKRenderingServer PROPERTY POSITION_INDEPENDENT_CODE ON)
target_link_libraries(InnoVKRenderingServer "${VULKAN}")